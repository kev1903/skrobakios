-- Check current RLS policies on stakeholder_contacts table
SELECT 
    schemaname,
    tablename,
    policyname,
    permissive,
    roles,
    cmd,
    qual,
    with_check
FROM pg_policies 
WHERE tablename = 'stakeholder_contacts'
ORDER BY policyname;